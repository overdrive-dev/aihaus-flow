## aih-effort

| SKILL | Location | Step | Label | Primary | Actor | Gate | Escape | Leverage | Reversibility | Drift Risk | Eligibility | Notes |
|-------|----------|------|-------|---------|-------|------|--------|----------|---------------|------------|-------------|-------|
| aih-effort | aih-effort/SKILL.md | Phase 1 — Read + inspect: context load | context-load | A | model | none | none | low | rev | easy | deterministic | H2-phase (numbered); silent load of project.md + decisions.md + knowledge.md; ADR-M012-A is binding; deterministic read |
| aih-effort | aih-effort/SKILL.md | Phase 1 — Read + inspect: distribution report | distribution-report | A | model | none | none | low | rev | easy | model-judgment | H2-phase (numbered); grep all 46 agent frontmatters; print GFM table (Agent/Model/Effort/Cohort); cohort lookup from annexes/cohorts.md; stop on --inspect; eligibility=model-judgment because GFM table construction + cost-estimate delta require model synthesis |
| aih-effort | aih-effort/SKILL.md | Phase 1 — Read + inspect: --status sidecar read | status-sidecar-read | A | model | none | none | low | rev | easy | deterministic | H2-phase (numbered); reads .aihaus/.effort; triggers v2→v3 migration if schema=2 sidecar present; print + exit; no commit; deterministic schema dispatch |
| aih-effort | aih-effort/SKILL.md | Phase 2 — Compute target distribution: --permission-mode guard | permission-mode-guard | A | model | none | none | low | rev | easy | deterministic | H2-phase (numbered); `--permission-mode` flag → exit nonzero with exact stderr: `--permission-mode flag removed in v0.16.0; use bash .aihaus/auto.sh for DSP launch`; checked before any other flag processing; deterministic error exit |
| aih-effort | aih-effort/SKILL.md | Phase 2 — Compute target distribution: preset-immune filter | preset-immune-filter | A | model | none | none | high | rev | med | deterministic | H2-phase (numbered); `is_preset_immune(cohort)` from pkg/scripts/lib/restore-effort.sh is single authoritative location; preset runs skip :adversarial-scout + :adversarial-review; explicit --cohort :adversarial-* or --agent <member> can mutate with literal-word `adversarial` confirm; deterministic — no NLP |
| aih-effort | aih-effort/SKILL.md | Phase 2 — Compute target distribution: diff compute | diff-compute | A | model | none | none | med | rev | med | deterministic | H2-phase (numbered); load cohort tuples from annexes/presets.md + expand via annexes/cohorts.md; compute file-by-file diff (target model+effort per agent); sonnet/haiku clip to effort:high on xhigh/max requests (ADR-M012-A §4); deterministic cohort expansion + comparison |
| aih-effort | aih-effort/SKILL.md | Phase 3 — Confirm + apply: one-question confirm | one-question-confirm | A | model | none | none | med | rev | easy | deterministic | H2-phase (numbered); single autonomy-protocol question: "Apply preset X (N agent edits across K cohorts)? y/enter"; no A/B/C menus; deterministic prompt shape |
| aih-effort | aih-effort/SKILL.md | Phase 3 — Confirm + apply: adversarial bypass | adversarial-bypass | A | model | none | none | high | rev | med | deterministic | H2-phase (numbered); explicit --cohort :adversarial-* or --agent <adversarial-member>: second prompt after step-10 with exact !! warning shape; any response other than literal `adversarial` aborts with no edits + no commit; deterministic literal-match gate |
| aih-effort | aih-effort/SKILL.md | Phase 3 — Confirm + apply: Edit apply | edit-apply | A | model | none | none | high | irrev | hard | deterministic | H2-phase (numbered); for each agent in diff: Edit model: old→target, Edit effort: old→target; if step-2 succeeds + step-3 fails: git checkout -- <file> before abort; never sed/awk; two Edit calls per file when both axes differ; deterministic per-file sequence |
| aih-effort | aih-effort/SKILL.md | Phase 4 — Commit + post-edit gate: explicit git add | explicit-git-add | A | model | none | none | med | irrev | easy | deterministic | H2-phase (numbered); NEVER git add -A (per feedback_worktree_merge_back_race.md); explicit path list from diff vector; deterministic path enumeration |
| aih-effort | aih-effort/SKILL.md | Phase 4 — Commit + post-edit gate: post-edit gate | post-edit-gate | A | model | none | none | high | irrev | hard | deterministic | H2-phase (numbered); `bash tools/smoke-test.sh && bash tools/purity-check.sh`; on non-zero: `git reset --hard HEAD~1`, print failing output, exit without retry; on green: print final delta + rollback one-liner; deterministic gate with self-revert |
| aih-effort | aih-effort/SKILL.md | Phase 4 — Commit + post-edit gate: sidecar write | sidecar-write | A | model | none | none | med | rev | easy | deterministic | H2-phase (numbered); after commit + gate pass: write .aihaus/.effort schema=3 (last_preset, last_commit, cohort.*.model/effort, per-agent lines); 17-guard skips if self-revert fired; 17-ownership: NEVER git add .aihaus/.effort (ADR-M009-A); adversarial-immune filter via is_preset_immune(); deterministic conditional write |
| aih-effort | aih-effort/annexes/cli-surface.md | CLI validation rules (ADR-M012-A) | cli-validation-rules | A | model | none | none | high | rev | med | deterministic | annex; --cohort single-axis REJECTED; --agent --model alone REJECTED; bare --model/--effort REJECTED; --permission-mode REJECTED (removed v0.16.0); --agent --effort Y ALLOWED; --agent --model X --effort Y ALLOWED; --preset <name> iterates cohort tuples + skips adversarial via is_preset_immune(); deterministic validation table; Location=annex |
| aih-effort | aih-effort/annexes/cli-surface.md | Phase-4 step 17 — v3 sidecar write | sidecar-write-annex | A | model | none | none | med | rev | easy | deterministic | annex; layout: schema=3, last_preset, last_commit, cohort.<name>.model/effort per cohort touched, per-agent override lines; write-time adversarial filter applied on preset (not on explicit --cohort/--agent); Location=annex |
| aih-effort | aih-effort/annexes/cohorts.md | Cohort membership table (46 agents → 6 cohorts) | cohort-membership-table | A | model | none | none | high | rev | med | deterministic | annex; single source of truth for cohort membership; 5-column parse contract (F-006): NF=7 per awk -F'|' on data rows; restore-effort.sh + install.ps1 parse f[3]=Agent, f[4]=Cohort; order change requires ADR amendment; :adversarial-scout + :adversarial-review are preset-immune; Location=annex |
| aih-effort | aih-effort/annexes/cohorts.md | Cohort reassignments (vs v0.15.0) | cohort-reassignments | A | model | none | none | low | rev | easy | model-judgment | annex; documents investigator→:doer absorption (3 agents), verifier-rich subset deletion, :planner split (17→4+13), :adversarial split (4→2+2); narrative rationale; eligibility=model-judgment because migration guidance requires NLP reading; Location=annex |
| aih-effort | aih-effort/annexes/presets.md | Preset Distribution Matrix | preset-distribution-matrix | A | model | none | none | high | rev | med | deterministic | annex; 3 presets × 6 cohorts → exact (model, effort) tuples; :adversarial-scout + :adversarial-review = preset-immune; :doer is only model-swap cohort (cost/balanced=sonnet, high=opus); balanced on clean v0.16.0 install = zero-diff no-op; deterministic lookup table; Location=annex |
| aih-effort | aih-effort/annexes/presets.md | Preset: cost | preset-cost | A | model | none | none | med | rev | easy | deterministic | annex; :planner-binding=(opus,high), :planner=(opus,high), :doer=(sonnet,high), :verifier=(haiku,medium), adversarial cohorts preset-immune; deterministic tuple set; Location=annex |
| aih-effort | aih-effort/annexes/presets.md | Preset: balanced | preset-balanced | A | model | none | none | med | rev | easy | deterministic | annex; :planner-binding=(opus,xhigh), :planner=(opus,high), :doer=(sonnet,high), :verifier=(haiku,high); applying on clean v0.16.0 = zero frontmatter diff; deterministic tuple set; Location=annex |
| aih-effort | aih-effort/annexes/presets.md | Preset: high | preset-high | A | model | none | none | med | rev | easy | deterministic | annex; :planner-binding=(opus,max), :planner=(opus,max), :doer=(opus,high) (model swap), :verifier=(haiku,high); sonnet/haiku per-agent overrides clip to effort:high; deterministic tuple set + clip rule; Location=annex |
| aih-effort | aih-effort/annexes/state-file.md | Schema v3 layout + field contract | state-file-schema-v3 | A | model | none | none | med | rev | easy | deterministic | annex; schema=3, last_preset enum (cost|balanced|high|custom), last_commit, cohort.*.model/effort, per-agent effort + model lines; no :colon on disk (CLI ergonomics only); values must not contain =; CRLF-stripped; blank+#-prefixed lines skipped; deterministic key-value parse contract; Location=annex |
| aih-effort | aih-effort/annexes/state-file.md | Migration v2 → v3 | state-file-migration | A | model | none | none | med | rev | med | deterministic | annex; triggered by /aih-update (silent) OR /aih-effort --status (idempotent); .calibration → .calibration.v2.bak + new .effort schema=3; permission_mode field dropped; cohort.planner.* split into planner-binding + planner; cohort.adversarial.* duplicated to both adversarial-scout + adversarial-review; authoritative implementation in pkg/scripts/lib/restore-effort.sh; Location=annex |
| aih-effort | aih-effort/annexes/renamed-from-aih-calibrate.md | Rename: aih-calibrate → aih-effort (v0.16.0 / M012 BREAKING) | rename-history | A | model | none | none | low | rev | easy | deterministic | annex; no backwards-compat shim; /aih-calibrate = skill-not-found; use /aih-effort for effort/model tuning; permission-mode moved to DSP wrapper (ADR-M014-A); migration recipe: rg -l '/aih-calibrate' . | xargs sed -i 's|/aih-calibrate|/aih-effort|g'; deterministic rename record; Location=annex |
