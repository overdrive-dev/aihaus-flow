# aihaus Harness (fixture: within caps — MUST PASS)

This fixture stays under both caps: well under 2048 bytes and 45 lines.
The smoke check (check_harness_byte_cap) must report it as passing the
byte/line cap helper; if it fails, the cap logic itself is broken.

- covered / gap / conflict / mechanics
- ask once; the answer becomes a rule

<!-- MAIN-SESSION-ONLY -->
routing line
<!-- /MAIN-SESSION-ONLY -->
