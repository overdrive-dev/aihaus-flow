# tools/

Maintainer-only scripts (CI, brand purity, regression, release-notes generator). These never ship to users — `install.sh` copies only `pkg/.aihaus/` and `pkg/templates/`.

See CLAUDE.md `## Releasing` for the generator workflow.
