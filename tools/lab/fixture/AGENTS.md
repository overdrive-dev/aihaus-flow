# Lab consumer instructions

This is a local acceptance fixture. Never deploy it or use credentials/external
services. Do not edit `eval/`; those tests are an external acceptance surface.
Product changes belong under `src/`. Run `node --test eval/counter.test.mjs`
before reporting completion.
