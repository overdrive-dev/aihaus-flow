# Built binaries
bin/

# Go module cache (not normally committed)
# vendor/ is intentionally NOT here — if vendoring is adopted, vendor/ stays tracked

# OS / editor noise
.DS_Store
*.swp
*~
.vscode/
.idea/

# Test outputs
*.test
*.out
coverage.out
coverage.html
